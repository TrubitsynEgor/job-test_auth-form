import { Typography } from 'antd'
import { MainLayout } from '../Layout'

const { Title } = Typography

export const Cart = () => {
  return (
    <MainLayout>
      <Title>Cart</Title>
    </MainLayout>
  )
}
